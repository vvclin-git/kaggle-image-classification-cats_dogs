## Quick start

Bootstrap the environment (sync deps + install this project in editable mode):

```powershell
./scripts/bootstrap.ps1
```

Then launch Jupyter from the repo root:

```powershell
jupyter lab
```

### Manual setup (if you prefer)

```powershell
uv sync
uv pip install -e .
```
